# Architecture (Sprint 1)

- **Front-end**: Next.js (App Router, TypeScript).
- **Mock API**: json-server (employees, attendance, leave).
- **State**: fetch langsung (tanpa global store), simple hooks di `web/src/lib`.
- **Auth (dummy)**:, login form tanpa backend (hardcoded).
- **Rute upgrade** (Sprint 2–3): tambah folder `api/` (Nest/Express + Prisma), pindahkan data & buat OpenAPI.
